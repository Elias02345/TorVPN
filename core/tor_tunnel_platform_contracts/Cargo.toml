[package]
name = "tor_tunnel_platform_contracts"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Fail-closed platform tunnel contracts for TorTunnel."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
