[package]
name = "tor_tunnel_leaktest"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Leak self-test scenarios and release gates for TorTunnel."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
