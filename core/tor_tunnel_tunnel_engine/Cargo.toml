[package]
name = "tor_tunnel_tunnel_engine"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Fail-closed tunnel engine policy for TorTunnel."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
